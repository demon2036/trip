# Stage 5: 特殊过滤（不限原因的关闭过滤）

**输入**:
- Stage 1 的输出（已在上下文中）
- Stage 3 的输出（已在上下文中）
- 日期窗口 → 参见 `prompts/globals.md`

**任务**: 纯逻辑步——删除在旅行日期内**有来源链接证实不可访问**的景点。
原因不限：季节性关闭、整修、事故/灾害封闭（例：园区因坠机等意外事故封锁，
与天气季节无关，一样删）、永久停业。判断标准只有一条：
链接证据显示旅行日期内进不去。

**被删除**:

| 名称 | 删除原因 | 证据链接 |
|---|---|---|
| 上高地 | 11月中~4月中全域关闭，12月不可访 | Stage 3 表中该行的来源链接 |

**保留列表**: Stage 1 原表去掉被删除项 = **最终景点列表**。

**禁止**: 不许加入新景点。`unknown` 的保留（不确定是否关闭的不删，标注待查）。
只删有链接证据证实关闭的——没有证据链接，就不许删。

---

完成后将完整结果（被删除表格 + 保留列表）输出，然后将最终景点列表写入 `output/plan/stage5_locked.md`。Part 1 完成。
